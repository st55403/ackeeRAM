package eu.golovkov.ackeeram.ui.theme

import androidx.compose.ui.graphics.Color

object RAMColor {
    //Light Mode
    val accentPrimary = Color(0xFF0000FF)

    //Backgrounds
    val backgroundsPrimary = Color(0xFFF4F4F9)
    val backgroundsSecondary = Color(0xFFE7E7EC)
    val backgroundsTertiary = Color(0xFFFFFFFF)
    val backgroundsBottomNav = Color(0xFFFFFFFF)

    //Foregrounds
    val foregroundsPrimary = Color(0xFF000000)
    val foregroundsSecondary = Color(0xFF666666)
    val foregroundsTertiary = Color(0xFFB3B3B3)
    val foregroundsSeparator = Color(0xFFF0F0F0)
    val foregroundsOnPrimary = Color(0xFFF0F0F0)

    //Icons
    val iconsPrimary = Color(0xFF000000) // same as foregroundsPrimary
    val iconsSecondary = Color(0xFFB3B3B3) // same as foregroundsTertiary
    val iconsTertiary = Color(0xFF0000FF) // same as accentPrimary

    //Dark Mode
    val accentPrimaryDm = Color(0xFF9595FE)

    //Backgrounds
    val backgroundsPrimaryDm = Color(0xFF181819)
    val backgroundsSecondaryDm = Color(0xFF2E2E2F)
    val backgroundsTertiaryDm = Color(0xFF474747)
    val backgroundsBottomNavDm = Color(0xFF0F0F10)

    //Foregrounds
    val foregroundsPrimaryDm = Color(0xFFFFFFFF)
    val foregroundsSecondaryDm = Color(0xFF8B8B8C)
    val foregroundsTertiaryDm = Color(0xFF5D5D5E)
    val foregroundsSeparatorDm = Color(0xFF474749)
    val foregroundsOnPrimaryDm = Color(0xFF000000)

    //Icons
    val iconsPrimaryDm = Color(0xFFFFFFFF) // same as foregroundsPrimaryDm
    val iconsSecondaryDm = Color(0xFFB3B3B3)
    val iconsTertiaryDm = Color(0xFF9595FE) // same as accentPrimaryDm
}